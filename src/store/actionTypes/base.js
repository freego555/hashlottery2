// Set all branches to default state

export const DEFAULT_STATE = '@lifeline/DEFAULT_STATE';

// Set network branch to default state

export const DEFAULT_NETWORK_BRANCH_STATE = '@lifeline/DEFAULT_NETWORK_BRANCH_STATE';
